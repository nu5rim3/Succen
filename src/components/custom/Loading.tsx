import loading from '../../assets/loading.svg'
const Loading = () => {
    return (
        <div className='bg-transparent h-screen flex justify-center items-center'><img src={loading} /></div>
    )
}

export default Loading